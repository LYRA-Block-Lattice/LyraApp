@page "/"
@page "/create-wallet"
@inject IJSRuntime JsWindow;

<div @ref="_react" />

<div id="root"></div>

<a href="/open-wallet">Open wallet</a>
<a href="/create-wallet">Create wallet</a>

@code {
    private ElementReference _react;

    protected override Task OnInitializedAsync()
    {
        return base.OnInitializedAsync();
    }

    protected override Task OnParametersSetAsync()
    {
        return base.OnParametersSetAsync();
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await JsWindow.InvokeVoidAsync("loadScript", "_content/ReactRazor/scripts/app.js");
            //await JsWindow.InvokeVoidAsync("bruic.openwallet", _react);
        }

        //await base.OnAfterRenderAsync(firstRender);
    }
}